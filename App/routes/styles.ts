import {StyleSheet} from 'react-native';
import COLOR from '../config/color';
import FONTS from '../config/fonts';

const styles = (state: any) => {
  let theme = state? state : 'light';
  return StyleSheet.create({
   

  });
};

export default styles;
